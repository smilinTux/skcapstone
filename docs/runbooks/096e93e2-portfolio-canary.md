# 096e93e2 simulation-only Portfolio Steward canary

Status: immutable candidate for independent review. This is not activation authority.

## Exact pins

- Base source commit: `73d5e294ab4b7e5d450375a983978b4e76e1107b`
- Base source tree: `819f3d150f2bc83f4cfc85f518b3748813d2fb72`
- Proposal card `d5c6f539`: reviewed tree `8fdb056dbeabf31f547dfef78a89975df74011af`, evidence SHA-256 `a18596c5768ef7be65b1e46ba90b9a59d9c8b56456a8bdbc4f51b5f6ef18b47e`
- Allocator card `2850e05b`: reviewed tree `bc8a01a2e5c24019a3dca8d8898702701b8bace3`, evidence SHA-256 `35b3066939763ab5745b2d5152d9ce2f45bc6de08562abc742e0f9a2adee08cc`
- Fenced writer card `048c5de2`: repair commit `bdec7b6c`, reviewed tree `e2ee55c8425cee25a269315556a9a95b305a4149`, evidence SHA-256 `bcf778da9956abadca9118dc84b8202436f6ba2fb4f49b907e2ccd7837ab6a64`
- Handoff producer card `de712b36`: reviewed tree `855f6afeb990415a46a57fc3757bf393e60b5e19`, evidence SHA-256 `66ad9067fbb068363594eb8f41dee2aece96b190faa55d4ffb2995340f6d5d11`
- Handoff conformance card `fe3877e5`: PASS review SHA-256 `7b2818aa7db38ed615c089884d8be4e253736e9245fbd1afdbabce6c3b99ea24`
- Policy: `portfolio-canary-simulation.v1`, fixture SHA-256 `aaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaaa`
- Schema fixture SHA-256: `bbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbbb`
- Selected card: `public-synthetic-canary-096e93e2`
- Selected card revision: `5a9c502585d2d932f0baed375bbbfba761706685621df24269971984f941bab2`
- Classification: `public-synthetic`
- WIP: expected active `0`, hard limit `1`
- Lease: generation `7`, duration `300` seconds
- Fence epoch: `11`
- Executor allowlist: `portfolio-canary-executor` only
- Reviewer: `portfolio-canary-independent-reviewer`, distinct from presenter, steward, allocator, writer, producer, and executor

The placeholder-looking policy and schema values are deliberate public-synthetic fixture bytes. They are not representations of a live policy registry. A live policy lookup is impossible through this front door.

## Separation and receipts

`PortfolioCanaryFrontDoor` accepts only validated `PortfolioPlanContentV1` and `AllocationDecisionV1`. It then creates an in-memory writer fence simulation, a simulated claim receipt, exact `ExecutionHandoffV1`, and an authenticated in-process handoff receipt. Presentation identity is absent from all authority objects. The producer, mutation simulator, executor, and reviewer names are distinct constants or exact manifest values.

The claim event is exactly `SIMULATION_NO_EVENT`. Authorization is exactly `SIMULATION_DENY_LIVE_MUTATION`. The module imports no CardStore, board, provider, HTTP, socket, credential, protected-data, subprocess, or external-action adapter.

## One-card proof and stop conditions

The front door proceeds only when the proposal contains exactly one recommendation and it is the manifest card at the exact revision. It abstains on:

- any widened or substitute selection;
- proposal hash mismatch;
- card or revision mismatch;
- policy mismatch or denial;
- WIP mismatch or exhaustion;
- stale lease generation;
- stale snapshot or allocation;
- executor outside the one-entry allowlist;
- ineligible allocation;
- conflicting reuse of an idempotency key.

Any attempted CardStore write, network or provider call, protected-data access, credential use, external action, or changed authority binding is an immediate stop condition. There is no adapter through which those effects can occur.

## Determinism and rollback

An in-process lock ensures concurrent identical calls return one byte-identical result and receipt. A conflicting replay abstains without replacing the original result. Every result explicitly counts one selected card and zero live claims, CardStore mutations, provider traffic, protected-data accesses, and external actions.

Rollback requires no migration or state repair. Discard the in-memory object, delete generated evidence, and revert exactly the three closed changed paths below. No lifecycle or CardStore event is generated by rollback.

## Closed changed-path manifest

1. `docs/runbooks/096e93e2-portfolio-canary.md`
2. `src/skcapstone/portfolio_canary.py`
3. `tests/test_portfolio_canary.py`

No merge, deployment, restart, credential operation, activation, live claim, provider request, protected-data read, or external action is part of this candidate.
