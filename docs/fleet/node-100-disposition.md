# .100 unit disposition (`node-100`, role `worker-gpu`)

Generated by `scripts/fleet/gen-node-disposition.py`. Do not hand-edit:
regenerate it. Epic `3bbf39ea`.

Collected read-only. Nothing on the node was changed to produce this.

| disposition | count |
|---|---|
| `disable` | 2 |
| `keep` | 22 |
| `out-of-scope` | 72 |
| **total** | **96** |

`present-on-control` compares against the control node (`node-noroc2027`, which is what `paths.self_node_name()` derives from the hostname; there is no `node-158`).

| unit | scope | present-on-control | disposition | rationale |
|---|---|---|---|---|
| `apparmor.service` | system | yes | out-of-scope | distro or desktop baseline |
| `apport-autoreport.path` | system | yes | out-of-scope | distro or desktop baseline |
| `apport-autoreport.timer` | system | yes | out-of-scope | distro or desktop baseline |
| `apport-forward.socket` | system | yes | out-of-scope | distro or desktop baseline |
| `apport.service` | system | yes | out-of-scope | distro or desktop baseline |
| `apt-daily-upgrade.timer` | system | yes | out-of-scope | distro or desktop baseline |
| `apt-daily.timer` | system | yes | out-of-scope | distro or desktop baseline |
| `blk-availability.service` | system | no | out-of-scope | distro or desktop baseline |
| `cloud-config.service` | system | yes | out-of-scope | distro or desktop baseline |
| `cloud-final.service` | system | yes | out-of-scope | distro or desktop baseline |
| `cloud-init-hotplugd.socket` | system | yes | out-of-scope | distro or desktop baseline |
| `cloud-init-local.service` | system | yes | out-of-scope | distro or desktop baseline |
| `cloud-init.service` | system | yes | out-of-scope | distro or desktop baseline |
| `comfyui-model-update.timer` | system | no | keep | keeps comfyui models current |
| `console-setup.service` | system | yes | out-of-scope | distro or desktop baseline |
| `containerd.service` | system | yes | keep | docker.service dependency |
| `dm-event.socket` | system | no | out-of-scope | distro or desktop baseline |
| `docker.service` | system | yes | keep | EVIDENCE-BACKED: docker ps shows the frigate NVR healthy and up 15 hours on :5000/:8554-8555/:8971. Out of SK scope but load-bearing for the household; not this epic's to remove |
| `docker.socket` | system | yes | keep | docker.service dependency |
| `dpkg-db-backup.timer` | system | yes | out-of-scope | distro or desktop baseline |
| `e2scrub_all.timer` | system | yes | out-of-scope | distro or desktop baseline |
| `e2scrub_reap.service` | system | yes | out-of-scope | distro or desktop baseline |
| `finalrd.service` | system | no | out-of-scope | distro or desktop baseline |
| `fsidd.service` | system | yes | out-of-scope | distro or desktop baseline |
| `fstrim.timer` | system | yes | out-of-scope | distro or desktop baseline |
| `getty@.service` | system | yes | out-of-scope | distro or desktop baseline |
| `gpu-manager.service` | system | yes | out-of-scope | hardware and platform baseline |
| `grub-common.service` | system | yes | out-of-scope | distro or desktop baseline |
| `grub-initrd-fallback.service` | system | yes | out-of-scope | distro or desktop baseline |
| `iscsid.socket` | system | no | out-of-scope | distro or desktop baseline |
| `keyboard-setup.service` | system | yes | out-of-scope | distro or desktop baseline |
| `lvm2-lvmpolld.socket` | system | no | out-of-scope | distro or desktop baseline |
| `lvm2-monitor.service` | system | no | out-of-scope | distro or desktop baseline |
| `lxd-installer.socket` | system | no | out-of-scope | distro or desktop baseline |
| `mdcheck_continue.timer` | system | no | out-of-scope | distro or desktop baseline |
| `mdcheck_start.timer` | system | no | out-of-scope | distro or desktop baseline |
| `mdmonitor-oneshot.timer` | system | no | out-of-scope | distro or desktop baseline |
| `motd-news.timer` | system | yes | out-of-scope | distro or desktop baseline |
| `multipathd.service` | system | no | out-of-scope | distro or desktop baseline |
| `multipathd.socket` | system | no | out-of-scope | distro or desktop baseline |
| `mxbai-arc.service` | system | no | keep | mxbai-embed-large on the Vulkan iGPU, :11438 |
| `networkd-dispatcher.service` | system | yes | out-of-scope | distro or desktop baseline |
| `nfs-blkmap.service` | system | yes | keep | nfs-server dependency |
| `nfs-client.target` | system | yes | keep | nfs-server dependency |
| `nfs-server.service` | system | yes | keep | EVIDENCE-BACKED: exportfs shows /srv/comfyui exported rw to 192.168.0.0/24 and comfyui writes to /mnt/comfyui-nfs/output. It is serving something real |
| `nvidia-cdi-refresh.path` | system | no | out-of-scope | hardware and platform baseline |
| `nvidia-cdi-refresh.service` | system | no | out-of-scope | hardware and platform baseline |
| `nvidia-hibernate.service` | system | no | out-of-scope | hardware and platform baseline |
| `nvidia-resume.service` | system | no | out-of-scope | hardware and platform baseline |
| `nvidia-suspend.service` | system | no | out-of-scope | hardware and platform baseline |
| `ollama.service` | system | yes | keep | the fleet embedding endpoint on :11434 |
| `open-iscsi.service` | system | no | out-of-scope | distro or desktop baseline |
| `pollinate.service` | system | no | out-of-scope | distro or desktop baseline |
| `remote-fs.target` | system | yes | keep | mounts /mnt/comfyui-nfs that comfyui writes into |
| `rpcbind.service` | system | yes | keep | nfs-server dependency |
| `rpcbind.socket` | system | yes | keep | nfs-server dependency |
| `setvtrgb.service` | system | yes | out-of-scope | distro or desktop baseline |
| `snapd.apparmor.service` | system | yes | out-of-scope | distro or desktop baseline |
| `snapd.autoimport.service` | system | yes | out-of-scope | distro or desktop baseline |
| `snapd.core-fixup.service` | system | yes | out-of-scope | distro or desktop baseline |
| `snapd.recovery-chooser-trigger.service` | system | yes | out-of-scope | distro or desktop baseline |
| `snapd.seeded.service` | system | yes | out-of-scope | distro or desktop baseline |
| `snapd.service` | system | yes | out-of-scope | distro or desktop baseline |
| `snapd.snap-repair.timer` | system | yes | out-of-scope | distro or desktop baseline |
| `snapd.socket` | system | yes | out-of-scope | distro or desktop baseline |
| `snapd.system-shutdown.service` | system | yes | out-of-scope | distro or desktop baseline |
| `ssh.socket` | system | yes | out-of-scope | distro or desktop baseline |
| `syncthing-resume.service` | system | yes | keep | resumes syncthing after suspend |
| `systemd-networkd-wait-online.service` | system | no | out-of-scope | distro or desktop baseline |
| `systemd-networkd.service` | system | no | out-of-scope | distro or desktop baseline |
| `systemd-networkd.socket` | system | no | out-of-scope | distro or desktop baseline |
| `systemd-pstore.service` | system | yes | out-of-scope | distro or desktop baseline |
| `systemd-resolved.service` | system | yes | out-of-scope | distro or desktop baseline |
| `systemd-timesyncd.service` | system | yes | out-of-scope | distro or desktop baseline |
| `tailscaled.service` | system | yes | keep | the only reachable path to this node |
| `thermald.service` | system | yes | out-of-scope | hardware and platform baseline |
| `ubuntu-fan.service` | system | yes | out-of-scope | distro or desktop baseline |
| `unattended-upgrades.service` | system | yes | out-of-scope | distro or desktop baseline |
| `comfyui.service` | user | no | keep | image generation on :8188 |
| `dirmngr.socket` | user | yes | out-of-scope | distro or desktop baseline |
| `f5-tts.service` | user | no | keep | voice synthesis on :18796 |
| `gpg-agent-browser.socket` | user | yes | out-of-scope | distro or desktop baseline |
| `gpg-agent-extra.socket` | user | yes | out-of-scope | distro or desktop baseline |
| `gpg-agent-ssh.socket` | user | yes | out-of-scope | distro or desktop baseline |
| `gpg-agent.socket` | user | yes | out-of-scope | distro or desktop baseline |
| `keyboxd.socket` | user | yes | out-of-scope | distro or desktop baseline |
| `launchpadlib-cache-clean.timer` | user | yes | out-of-scope | distro or desktop baseline |
| `pk-debconf-helper.socket` | user | yes | out-of-scope | distro or desktop baseline |
| `qwen3-arc.service` | user | no | keep | qwen3.5:4b on :8085 |
| `session-migration.service` | user | yes | disable | Ubuntu session migration one-shot; a headless worker has no desktop session to migrate |
| `skai-beellama-restart.timer` | user | no | keep | keeps the sk-default server healthy |
| `skai-beellama.service` | user | no | keep | serves ornith-1.0-9b on :8082, the fleet sk-default |
| `skvoice.service` | user | yes | disable | EVIDENCE-BACKED: enabled but ActiveState=inactive, SubState=dead. An enabled unit that never runs is exactly the half-alive duplicate class behind the comms pileups. Retire it |
| `sovereign-orchestrator.service` | user | no | keep | call orchestration on :18801 |
| `syncthing.service` | user | yes | keep | STAYS, but its folder set shrinks to skfleet-control only (card ddb2a02f); it is also required to unshare the sovereign folder in an orderly way rather than by deletion |
| `whisper-stt.service` | user | no | keep | speech to text on :18794 |

## Evidence for the three judgement calls

### `nfs-server.service`: KEEP. It is serving something.

```console
$ exportfs -v
/srv/comfyui  192.168.0.0/24(sync,wdelay,hide,no_subtree_check,sec=sys,rw,
                             secure,no_root_squash,no_all_squash)
EOEXPORTS     <world>(sync,wdelay,hide,no_subtree_check,sec=sys,ro,secure,
                      root_squash,no_all_squash)
```

`/srv/comfyui` is exported read-write to the whole LAN, and `comfyui.service`
runs with `--output-directory /mnt/comfyui-nfs/output`. The export is live and
load-bearing, so nfs-server stays along with `rpcbind`, `nfs-blkmap`,
`nfs-client.target` and `remote-fs.target`.

🔴 **Two defects found in that output, neither in scope for this card:**

1. **`EOEXPORTS` is being exported to `<world>`.** That is a heredoc
   terminator that leaked into `/etc/exports` and is now a real export entry.
   `exportfs` even warns about it: `No host name given with EOEXPORTS`. It is
   read-only and root-squashed, so the exposure is limited, but it is an
   unintended world-facing NFS export on a box reachable from the LAN.
2. **`/srv/comfyui` is exported `no_root_squash`** to `192.168.0.0/24`. Any
   LAN host that can mount it writes as root.

Both belong on a new security card, not on the slim.

### `docker.service`: KEEP. It is running the household NVR.

```console
$ docker ps
CONTAINER ID   IMAGE                                     STATUS                  NAMES
064037aa4162   ghcr.io/blakeblackshear/frigate:stable    Up 15 hours (healthy)   frigate
```

Ports 5000, 8554-8555 (tcp and udp) and 8971. `docker ps -a` shows no other
containers, running or stopped. Frigate is outside SK scope, but it is live
and this epic has no mandate to take down Chef's cameras. Keep, with
`docker.socket` and `containerd.service`.

### `skvoice.service`: DISABLE. Enabled but dead.

```console
$ systemctl --user show skvoice.service -p ActiveState -p SubState --value
inactive
dead
```

Enabled at boot, never running. That is exactly the half-alive duplicate
class behind the comms pileups and oomd freezes. Retire it; the generated
revert script re-enables it in one line if that call is wrong.

### `syncthing.service`: KEEP, with a shrunken folder set.

It stays because it is the only orderly way to **unshare** the sovereign
folder (card `3118769c`); deleting `~/.skcapstone` on .100 while it is still
shared would propagate the delete to .158, .41 and noroc2027. After the split
its folder set is exactly one entry: `skfleet-control`. The folder it loses
is `skcapstone-sync` (`SKCapstone Sovereign`, 5.0G on this node across 29
agent directories). See `docs/fleet/control-bus-folder.md`.

### No cron surface

```console
$ which crontab
(no output)
```

.100 has no crontab binary installed, so there is nothing to remove there.
