- Card `ca220923`: builder dispatch coalesces string workspace bindings (`repository`, `base_ref`, `base_revision`) from card metadata and folded links. Links-only, metadata-only, and matching dual bindings dispatch. Conflicting or non-string bindings fail closed, and consumer revalidation includes current folded links so post-offer amendments block execution. Review lineage fields are never treated as workspace aliases.
