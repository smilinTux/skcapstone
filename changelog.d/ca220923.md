- Card `ca220923`: builder dispatch now coalesces workspace source bindings (repository, base_ref, base_revision) from card meta AND folded card links. Links-only cards dispatch; meta-only cards dispatch; matching dual-source cards dispatch. When both sources exist and differ, `offer` fails closed with `source binding conflict: <field>`. `materialize` now revalidates the current card so a link amendment after the offer blocks materialization with `amended after offer`. `link_source_card` / `link_head_revision` are NOT treated as workspace aliases. Malformed source values still rejected.
