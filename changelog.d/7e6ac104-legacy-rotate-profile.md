- Card `7e6ac104`: forbid the legacy generic rotation timer in the generated
  seat-cycle profile so convergence disables it and stops its paired service
  before activating the serialized scheduler.
