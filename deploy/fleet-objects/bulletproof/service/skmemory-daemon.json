{
  "kind": "service",
  "labels": {
    "epic": "CR-7.3",
    "tier": "core"
  },
  "name": "skmemory-daemon",
  "spec": {
    "conditions": [
      "Ready",
      "Progressing",
      "CrashLooping",
      "EmbedReachable"
    ],
    "dependsOn": [
      "config/skmem-pg-local (node health condition, not a Service)"
    ],
    "failover": "manual",
    "nodeSelector": {
      "always-on": "true"
    },
    "note": "skmem-pg itself stays OUT of fleet management (spec 5.2): it is a node-local probe_conditions health surface only, never actuated. EmbedReachable = fail-loud instead of NULL embeddings.",
    "resources": {
      "cores": 1,
      "ram_gb": 1.0
    },
    "restartPolicy": "on-failure",
    "revert": "restore prior spec generation: re-apply the previous manifest generation (skfleet apply -f the prior JSON; write_spec bumps generation, so the prior generation is the authoritative rollback target). Declarative and reversible by spec regeneration; no imperative rollback script.",
    "runtime": "systemd-user",
    "source": "skmemory/docs/deploy-plan/skmemory-bulletproof-deploy.md",
    "unit": "skmemory-daemon.service"
  }
}
