{
  "kind": "service",
  "labels": {
    "epic": "CR-7.3",
    "tier": "core"
  },
  "name": "skgateway-claude-wrapper",
  "spec": {
    "conditions": [
      "Ready",
      "CrashLooping",
      "WrapperReachable"
    ],
    "failover": "manual",
    "healthCheck": {
      "port": 18782
    },
    "nodeSelector": {
      "control-plane": "true"
    },
    "note": "SPOF: all .158 Anthropic/claude-* traffic depends on this unmanaged wrapper (G5). Modelled so it is health-integrated and fails loud; failover manual + alert (no fallback backend yet).",
    "resources": {
      "cores": 1,
      "ram_gb": 0.5
    },
    "restartPolicy": "on-failure",
    "revert": "restore prior spec generation: re-apply the previous manifest generation (skfleet apply -f the prior JSON; write_spec bumps generation, so the prior generation is the authoritative rollback target). Declarative and reversible by spec regeneration; no imperative rollback script.",
    "runtime": "systemd-user",
    "source": "skgateway/docs/deploy-plan/skgateway-bulletproof-deploy.md",
    "unit": "claude-code-api.service"
  }
}
