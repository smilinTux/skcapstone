{
  "kind": "service",
  "labels": {
    "epic": "CR-7.3",
    "tier": "core"
  },
  "name": "skcapstone-daemon",
  "spec": {
    "conditions": [
      "Ready",
      "Progressing",
      "CrashLooping",
      "MemoryBounded"
    ],
    "failover": "manual",
    "nodeSelector": {
      "control-plane": "true"
    },
    "note": "MemoryMax/StartLimit caps are the .41 OOM-freeze fix (G2); MemoryBounded surfaces the cap. Type=notify WatchdogSec=300 upstream. Per-agent template unit.",
    "resources": {
      "cores": 1,
      "ram_gb": 1.5
    },
    "restartPolicy": "on-failure",
    "revert": "restore prior spec generation: re-apply the previous manifest generation (skfleet apply -f the prior JSON; write_spec bumps generation, so the prior generation is the authoritative rollback target). Declarative and reversible by spec regeneration; no imperative rollback script.",
    "runtime": "systemd-user",
    "source": "skcapstone/docs/deploy-plan/skcapstone-bulletproof-deploy.md",
    "unit": "skcapstone@lumina.service"
  }
}
