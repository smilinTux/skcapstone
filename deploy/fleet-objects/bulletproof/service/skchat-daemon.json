{
  "kind": "service",
  "labels": {
    "epic": "CR-7.3",
    "tier": "app"
  },
  "name": "skchat-daemon",
  "spec": {
    "conditions": [
      "Ready",
      "Progressing",
      "CrashLooping"
    ],
    "failover": "manual",
    "nodeSelector": {
      "always-on": "true"
    },
    "note": "~/.skchat chat history + group keys have zero backup path (theme 5); restoreBeforeStart + skchat-backup cron close it.",
    "resources": {
      "cores": 1,
      "ram_gb": 1.0
    },
    "restartPolicy": "on-failure",
    "restoreBeforeStart": true,
    "revert": "restore prior spec generation: re-apply the previous manifest generation (skfleet apply -f the prior JSON; write_spec bumps generation, so the prior generation is the authoritative rollback target). Declarative and reversible by spec regeneration; no imperative rollback script.",
    "runtime": "systemd-user",
    "source": "skchat/docs/deploy-plan/skchat-bulletproof-deploy.md",
    "unit": "skchat-daemon.service"
  }
}
