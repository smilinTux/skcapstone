{
  "kind": "service",
  "labels": {
    "epic": "CR-7.3",
    "tier": "core"
  },
  "name": "capauth-keystore",
  "spec": {
    "compose": {
      "service": "capauth-keystore"
    },
    "conditions": [
      "Ready",
      "CrashLooping",
      "RootCustodyVerified"
    ],
    "failover": "manual",
    "nodeSelector": {
      "control-plane": "true"
    },
    "note": "keystore docker volume has no backup path (theme 5); RootCustodyVerified ties to the capauth-custody-doctor cron. Revocation/expiry gap (G1) is a code fix, tracked separately.",
    "resources": {
      "cores": 1,
      "ram_gb": 0.5
    },
    "restartPolicy": "on-failure",
    "restoreBeforeStart": true,
    "revert": "restore prior spec generation: re-apply the previous manifest generation (skfleet apply -f the prior JSON; write_spec bumps generation, so the prior generation is the authoritative rollback target). Declarative and reversible by spec regeneration; no imperative rollback script.",
    "runtime": "docker",
    "source": "capauth/docs/deploy-plan/capauth-bulletproof-deploy.md",
    "unit": "capauth-keystore"
  }
}
