# SKDashboard-owned shutdown bound. Uvicorn drains for at most 10 seconds;
# systemd allows five more seconds for process teardown before enforcing stop.
# Rollback: remove this drop-in and run `systemctl --user daemon-reload`.
[Service]
KillSignal=SIGINT
TimeoutStopSec=15s
